# Camera
persist.vendor.camera.perfcapture=1

# Display postprocessing
persist.ppd.fde.config=0

# Fingerprint
ro.hardware.fp.fpc=true
ro.hardware.fp.goodix=true

# Performance mode
persist.sys.reloaded.perfmode=1

# Face Unlock
ro.face.moto_unlock_service=true
ro.face.moto_unlock_service.cam_id=5
